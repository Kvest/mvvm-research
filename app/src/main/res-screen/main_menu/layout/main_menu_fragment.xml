<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable name="viewModel" type="com.kvest.mvvm_research.screen.main_menu.MainMenuFragment.ViewModel"/>
    </data>
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dip">
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="@{v -> viewModel.showCounterScreen(context)}"
            android:text="Show counter"/>
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="@{v -> viewModel.showUserInfo(context)}"
            android:text="Show user info"/>
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="@{v -> viewModel.showListScreen(context)}"
            android:text="Show list"/>
    </LinearLayout>
</layout>
